<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Always include this permission -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Include only if your app benefits from precise location access. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pesquisa_Eleitoral">
        <activity
            android:name=".EleitoresActivity"
            android:exported="false" />
        <activity
            android:name=".FinalizarActivity"
            android:exported="false" />
        <activity
            android:name=".PesquisaProblemasActivity"
            android:exported="false" />
        <activity
            android:name=".PesquisaEspontaneaActivity"
            android:exported="false" />
        <activity
            android:name=".PesquisaEstimuladaActivity"
            android:exported="false" />
        <activity
            android:name=".MenuResultadosActivity"
            android:exported="false" />
        <activity
            android:name=".MenuPesquisasActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>